<nav>
<div class="nav-wrapper pink accent-1">
  <%= link_to 'Goober', root_path, class:"brand-logo center"%>
  <ul id="nav-mobile" class="right hide-on-med-and-down">
    <% if current_user %>
      <li> Welcome, <%= current_user.name %> </li>
      <% if current_user.role == "rider" %>
      <li> <%= link_to "Dashboard", rider_path(current_user)%> </li>
      <% else %>
      <li> <%= link_to "Dashboard", driver_path(current_user)%> </li>
      <% end %>
      <li> <%= link_to 'Logout', logout_path, method:
        :delete %> </li>
    <% else %>
      <li><%= link_to 'Login', login_path %>
    <% end %>
  </ul>
</div>
</nav>

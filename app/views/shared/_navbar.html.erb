<nav>
<div class="nav-wrapper pink accent-1">
  <a href="#" class="brand-logo center">Goober</a>
  <ul id="nav-mobile" class="right hide-on-med-and-down">
    <% if current_user %>
      <li> Welcome, <%= current_user.name %> </li>
      <li> <%= link_to 'Logout', logout_path, method:
        :delete %> </li>
    <% else %>
      <li><%= link_to 'Login', login_path %>
    <% end %> 
  </ul>
</div>
</nav>
